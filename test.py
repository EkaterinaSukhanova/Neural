def vectorize(func):
    def wrapper(*args, **kwargs):
        for i in range(0, len())
            func(*args, **kwargs)

    return wrapper


def myfunc(a, b):
    if a > b:
        return a - b
    else:
        return a + b

